{
  "name": "@radar/relay-types",
  "version": "3.0.0-beta",
  "description": "TypeScript type definitions for the Radar Relay API and SDK.",
  "author": "RadarTech",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RadarTech/relay-types.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "clean": "rimraf dist",
    "test": "echo \"No Tests Defined\" && exit 0",
    "lint": "tslint --fix --project .",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@0x/types": "^2.5.0-beta.2",
    "@0x/utils": "^4.6.0-beta.2"
  },
  "devDependencies": {
    "@radar/tslint-config": "^5.0.1",
    "@types/node": "^11.9.1",
    "prettier": "^1.14.0",
    "rimraf": "^2.6.3",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  }
}
